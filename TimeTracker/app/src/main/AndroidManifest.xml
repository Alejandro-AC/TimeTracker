<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.joans.timetracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LlistaActivitatsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LlistaIntervalsActivity">
        </activity>

        <service
            android:name="GestorArbreActivitats"
            android:enabled="true" >
        </service>

        <activity
            android:name=".NouProjecte">
        </activity>

        <activity
            android:name=".NovaTasca">
        </activity>

        <activity
            android:name=".InformacioActivity" >
        </activity>

        <activity
            android:name=".NouInforme" >
        </activity>

    </application>

</manifest>